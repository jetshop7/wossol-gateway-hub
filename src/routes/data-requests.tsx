import { createFileRoute } from "@tanstack/react-router";
import { AlertTriangle } from "lucide-react";

export const Route = createFileRoute("/data-requests")({ head: () => ({ meta: [{ title: "Data Requests | Wossol Platform" }, { name: "description", content: "Information about the future Wossol Platform data request process." }] }), component: DataRequests });

function DataRequests() { return <section className="section-pad bg-background"><div className="container-page max-w-3xl"><p className="text-sm font-semibold uppercase tracking-[0.18em] text-gold">Data requests</p><h1 className="mt-4 text-4xl font-semibold tracking-tight text-navy">Data deletion and privacy requests</h1><p className="mt-5 text-base leading-relaxed text-muted-foreground">This page reserves a stable public location for Wossol Platform data requests. The operational process is not yet published because the responsible legal entity, monitored privacy contact, verification steps, and fulfillment workflow still need to be confirmed.</p><div className="mt-8 rounded-xl border border-amber-300 bg-amber-50 p-6 text-amber-950"><AlertTriangle className="h-5 w-5" /><h2 className="mt-3 text-lg font-semibold">Not ready for production publication</h2><p className="mt-2 text-sm leading-relaxed">Do not use this page as a deletion endpoint until those details are resolved. See the <a href="/privacy-policy" className="font-semibold underline">Privacy Policy</a> for the current data-handling scope.</p></div></div></section>; }
